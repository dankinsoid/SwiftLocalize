// @ai-generated(guided)
import Foundation

public extension Fluent {

	/// Transitional alias kept while call-sites still spell the locale tag as `Fluent.Tag`.
	/// The canonical type is `Language` — prefer that in new code. This alias exists so the
	/// Fluent runtime, tests, and downstream code keep compiling during the migration.
	typealias Tag = Language
}
